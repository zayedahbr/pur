{
  "name": "purewerk",
  "version": "1.0.0",
  "description": "PureWerk — stickers personnalisés avec paiement Stripe et vérification d'authenticité",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "stripe": "^17.0.0",
    "resend": "^4.0.0",
    "micro": "^10.0.1"
  },
  "engines": {
    "node": ">=18.x"
  }
}
